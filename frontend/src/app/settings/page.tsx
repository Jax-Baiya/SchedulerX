"use client";

import { getSettings, updateSettings } from "@/lib/api/settings";

export { default } from "@/modules/settings";

// Removed in-file SettingsPage implementation to avoid duplicate default export.
